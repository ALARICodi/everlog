# everlog · 你现在在**版本1**(`Desktop\everlog`,分支 `main`)

贴一篇文章 → 永久存进 Arweave,哈希锚进比特币区块。
**任何人都能独立验证「谁在什么时候写的」,不需要相信这个网站,也不需要它还活着。**

> 这份文件是**索引,不是手册**。它只写代码里看不出来的东西。
> 具体怎么实现的、为什么这么实现,去读代码注释和 `git log` —— 那两处永不过期,这里会。

---

## 先跑这一条

```bash
npm run status
```

现有几篇文章、各自上链到哪一步、账号和密钥多少、依赖齐不齐 —— 全在里面。

**不要相信任何文档里写的「目前状态」**(包括这一份),状态一律现查。

---

## 两个版本

| | 目录 | 端口 | 区别 |
|---|---|---|---|
| **版本1** | `Desktop\everlog` | 8000 | 无账号,任何人直接发 |
| **版本2** | `Desktop\everlog-v2` | 8001 | 版本1 + 账号 + 分享密钥 |

**关系:版本2 = 版本1 + 账号系统。发布链路、规范格式、证明、验证器一个字节都没改。**
两个版本产出的证明完全通用,同一个验证器都能验。

刻意分成两个目录、两套数据、两个 git 仓库,因为**以后可能分别部署到两个不同的永久网址**。
改动共有部分(`lib/canonical.js`、`lib/ots.js`、`lib/bundle.js` 等)时,**记得两边都要改**。

版本1 已打 tag `v1`,远端 `github.com/ALARICodi/everlog`。

---

## 红线

违反下面任何一条,都会造成**不可逆**的损失。动手前先确认不碰这些。

**① 规范格式已冻结。**
`everlog/1` 和 `everlog/2` 必须永远保持可验证。改 `lib/canonical.js` 里任何一条规范化规则,
= 已发布文章的证明**全部作废**。要加字段只能出 `everlog/3` 并存。

**② 上链不可撤销。**
写进 Arweave 的东西,你、我、任何人都删不掉,而且是用本站钱包签的名。
所以任何触碰发布链路的改动,**先在不联网的路径上验证**。

**③ `data/` 会自动提交推送到公开仓库。**
密码、密钥、个人信息**绝不能**放进去 —— 进了 git 历史就删不干净。
版本2 的用户数据在 `private/`(已 gitignore),别挪。

**④ `data/` 下的文件必须按二进制处理。**
`.gitattributes` 已锁死。`article.txt` 是哈希原像,git 的换行转换会让每行多一字节、
所有比特币证明当场作废。**这不是理论风险,实测撞到过。**

**⑤ 发布链路必须幂等。**
崩溃恢复会重跑 `publish()`。任何一步没做「是不是已经做过了」的判断,
恢复时就会把 Arweave 再传一遍 —— 同一篇文章两份永久记录,删不掉。

---

## 常用命令

```bash
npm run status         # 现状(先跑这个)
npm start              # 起服务
node jobs/upgrade.js   # 手动跑一次证明升级(服务内已挂 20 分钟定时)
```

起服务要用 **detached** 方式,别挂在 bash 会话下 —— 会话一结束进程就死:

```bash
powershell -Command "Start-Process node -ArgumentList 'server.js' -WorkingDirectory '<项目路径>' -WindowStyle Hidden"
```

公网预览用 cloudflared 隧道,网址每次重启都变,发给用户前**必须真访问一次确认能通**
(隧道能注册出网址但连不上边缘节点是常见情况,只看日志会误报成功)。

---

## 想知道「为什么」,去哪找

| 问题 | 去哪 |
|---|---|
| 某个决定为什么这么做 | `git log --grep 关键词` —— 每条 commit 都写了理由和被否决的方案 |
| 某段代码为什么这么写 | 读那个文件顶部的注释,**不要猜** |
| 上限为什么是 96 KiB 不是 100 | `lib/canonical.js` |
| 时间下界为什么用 MTP 不用区块时间 | `lib/anchors.js` |
| 为什么自己写 SHA-256 和 zip | `lib/sha256.js`、`lib/zip.js` |
| 为什么 `.ots` 也要传 Arweave | `jobs/upgrade.js` |
| 为什么信封锁在服务端 | `server.js` 搜「信封」 |
| 姓名为什么存四份(仅版本2) | `lib/users.js` |

---

## 环境的坑(会咬人,且不在代码里)

**OTS 客户端在 Windows 上装完直接崩** —— `python-bitcoinlib` 找不到 `ssl.dll`。
已修:`~/.claude/scripts/otslib/ssl.dll`(libcrypto 改的名),运行前挂进 PATH。`lib/ots.js` 自动处理。

**`ots verify` 要求本地跑比特币全节点**(几百 GB),服务器上永远失败。
所以判定确认用的是 `ots info` 读区块号 + 区块浏览器查时间。别改回去。

**`UID` 是 bash 的只读变量。** 测试脚本里拿它当变量名会被系统值悄悄覆盖,
导致一整轮「假失败」。这个坑浪费过一次排查。

**npm 从官方源常被掐断**(ECONNRESET)。用 `registry.npmmirror.com`。

---

## 刻意没做的事

**下面这些不是疏漏,是决定。要改先问陈志平。**

- **署名未经任何验证。** `author:` 就是个自由文本框,任何人能用任何名字发文。
  这个产品目前只证明「这段字节在某时刻存在过」,**没碰「谁写的」**。
  已知情,计划以后用**生物识别锚定**解决。真做时要出 `everlog/3`(公钥 + 签名)。
- **上链前的内容审核已被要求拆掉**(`lib/moderate.js` 保留未接线)。
  因此任何人提交的任何内容都会直接永久上链,由本站钱包签名。
- **版本2 的人脸识别只有占位,没有实现。** 活体检测、特征提取、模板存储、防照片攻击
  是独立模块,以后单独做。流程能走通,但那一步什么也不验。
- **验证器不检查 Arweave 那半。** 不是缺陷:手里有 zip 时,Arweave 只是同一份东西的
  另一个副本,对验证零贡献。它的角色是保存和分发,不是验证。
- **匿名发布的文章永远无法事后认领。** 归属在发布那一刻定死,这是有意的。

---

## 下一步

**部署。** 现在两个站都跑在陈志平的电脑上,靠随时会断的临时隧道。方案已经定了:

```
Render 免费档     跑网站(闲置休眠,有人访问才唤醒,够用)
GitHub Actions    跑升级任务(免费,永不休眠 —— 服务器休眠时进程内定时器不跑,
                  下载按钮就永远不会亮,这是个真陷阱)
GitHub 仓库       数据的唯一真相,服务器启动时拉一份
GitHub Pages      静态只读镜像 —— 服务器挂了文章照样能读能下载
```

要注意服务器和 Actions 都会写同一个仓库,推之前必须先拉。
